apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.chartDiscovery.name }}
  labels:
    {{- include "chart.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.chartDiscovery.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
